{
  "name": "websocket-experiment",
  "version": "0.0.1",
  "description": "A simple websocket experiment",
  "workspaces": [
    "server/*",
    "simple-svelte/*",
    "simple-next/*"
  ],
  "dependencies": {
    "socket.io": "^4.7.4",
    "socket.io-client": "^4.7.4"
  },
  "devDependencies": {
    "typescript": "^5",
    "@biomejs/biome": "1.5.3",
    "@types/node": "^20"
  },
  "scripts": {
    "svelte:dev": "bun run --cwd=simple-svelte dev",
    "next:dev": "bun run --workspace=simple-next dev"
  }
}